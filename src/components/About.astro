---
const stats = [
  { number: '2+', label: 'Years Learning' },
  { number: '15+', label: 'Projects Built' },
  { number: '100%', label: 'Eager to Learn' },
  { number: '24/7', label: 'Coding Passion' },
];
---

<section id="about" class="section">
  <div class="container">
    <div class="grid-brutal">
      <!-- Main content -->
      <div class="brutal-card rotate-1">
        <h2 class="text-huge about-title">ABOUT</h2>
        <div class="stack">
          <p class="text-body about-text">
            I'm a passionate junior developer eager to learn and grow in the
            world of web development. I focus on writing clean, functional code
            and creating user-friendly experiences. Every project is an
            opportunity to improve my skills and contribute meaningful
            solutions.
          </p>
          <p class="text-body about-text">
            Currently exploring modern web technologies and best practices. I'm
            committed to continuous learning, building projects that matter, and
            collaborating with teams to create impactful digital experiences.
          </p>
        </div>
      </div>

      <!-- Stats grid -->
      <div class="stats-grid">
        {
          stats.map((stat, index) => (
            <div
              class="brutal-card stat-card"
              style={`animation-delay: ${index * 0.1}s`}
            >
              <div class="text-big stat-number">{stat.number}</div>
              <div class="text-small stat-label">{stat.label}</div>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Philosophy section -->
    <div class="philosophy-section">
      <div class="brutal-card -rotate-1 philosophy-card">
        <h3 class="text-big philosophy-title">APPROACH</h3>
        <blockquote class="text-body philosophy-quote">
          "The best way to learn is by doing. Every challenge is an opportunity
          to grow, and every line of code is a step forward in the journey."
        </blockquote>
        <cite class="text-small philosophy-cite">
          - My Development Philosophy
        </cite>
      </div>
    </div>
  </div>
</section>

<script>
  // Intersection Observer for animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px',
  };

  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate');
      }
    });
  }, observerOptions);

  // Observe all cards in this section
  document.querySelectorAll('#about .brutal-card').forEach(card => {
    observer.observe(card);
  });
</script>
